/**
 * A Unary Function is a function that takes exactly one argument.
 *  The term "unary" comes from the number of arguments (arity),
 *  where unary means "one."


 */

function square(num){
    return num * num;
}

console.log(square(4));